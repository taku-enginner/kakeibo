<%# app/views/devise/sessions/new.html.erb (ログイン画面) %>
<%# ※ Sign upのコードをベースに、Login用に修正してあります %>

<div class="container my-5">
    <div class="row justify-content-center">
        <%# PCでは幅を狭くして目立たせる (col-md-6) %>
        <div class="col-12 col-md-8 col-lg-5">

            <div class="card shadow-lg p-4">
                <div class="card-body">

                    <h2 class="card-title text-center mb-4">🚪 ログイン</h2>

                    <%# Deviseのフォームヘルパーを使用 %>
                    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

                        <%# 💡 エラーメッセージの表示 (Devise共通) %>
                        <%= render "devise/shared/error_messages", resource: resource %>

                        <%# --------------------- Email --------------------- %>
                        <div class="mb-3">
                            <%= f.label :email, class: "form-label" %>
                            <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control form-control-lg" %>
                        </div>

                        <%# --------------------- Password --------------------- %>
                        <div class="mb-3">
                            <%= f.label :password, class: "form-label" %>
                            <%= f.password_field :password, autocomplete: "current-password", class: "form-control form-control-lg" %>
                        </div>

                        <%# --------------------- Remember Me --------------------- %>
                        <% if devise_mapping.rememberable? %>
                            <div class="form-check mb-3">
                                <%= f.check_box :remember_me, class: "form-check-input" %>
                                <%= f.label :remember_me, "次回から自動ログイン", class: "form-check-label" %>
                            </div>
                        <% end %>

                        <%# --------------------- ログインボタン --------------------- %>
                        <div class="actions mb-3">
                            <%= f.submit "Log in", class: "btn btn-primary btn-lg w-100" %>
                        </div>
                    <% end %>

                    <hr>

                    <%# サインアップ/パスワード忘れなどのリンク %>
                    <div class="text-center">
                        <%= render "devise/shared/links" %>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
