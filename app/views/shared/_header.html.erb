<%# app/views/shared/_header.html.erb %>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container-fluid">

        <%# ブランド名/ホームリンク %>
        <%= link_to '💰 家計簿アプリ', root_path, class: "navbar-brand" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">

                <%# 1. レシート登録 %>
                <li class="nav-item">
                    <%= link_to 'レシート登録', new_receipt_path, class: "nav-link" %>
                </li>

                <%# 2. カレンダー %>
                <li class="nav-item">
                    <%= link_to 'カレンダー', calendars_path, class: "nav-link" %>
                </li>

                <%# 3. カテゴリ一覧 %>
                <li class="nav-item">
                    <%= link_to 'カテゴリ一覧', receipt_categories_path, class: "nav-link" %>
                </li>

                <%# --------------------- 認証リンクの切り替え --------------------- %>
                <% if user_signed_in? %>

                    <%# ユーザー名表示 (オプション) %>
                    <li class="nav-item">
                        <span class="nav-link text-white-50">
                            ようこそ, <%= current_user.email.split('@').first %>
                        </span>
                    </li>

                    <%# ログアウト %>
                    <li class="nav-item">
                        <%= link_to 'ログアウト', destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-light ms-2" %>
                    </li>
                <% else %>
                    <%# ログイン %>
                    <li class="nav-item">
                        <%= link_to 'ログイン', new_user_session_path, class: "btn btn-outline-success ms-2" %>
                    </li>
                <% end %>
                <%# ---------------------------------------------------------------- %>

            </ul>
        </div>
    </div>
</nav>
