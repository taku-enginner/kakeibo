<div class="card shadow-sm mt-4">
    <div class="card-header bg-info text-white">
        <h5 class="mb-0">📅 レシート一覧 (メモと日付)</h5>
    </div>

    <div class="card-body p-0">
        <% if @receipts.empty? %>
            <p class="p-3 text-muted text-center">まだレシートの記録がありません。</p>
        <% else %>
            <div class="table-responsive">
                <table class="table table-striped table-hover mb-0">
                    <thead class="table-light">
                        <tr>
                            <th scope="col" style="width: 15%;">日付</th>
                            <th scope="col">メモ</th>
                            <th scope="col" class="text-end" style="width: 15%;">操作</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @receipts.each do |receipt| %>
                            <tr>
                                <%# 💡 regist_date (登録日/日付) の表示 %>
                                <td class="text-nowrap">
                                    <%= receipt.regist_date&.strftime("%Y/%m/%d") || "日付不明" %>
                                </td>

                                <%# 💡 memo (メモ) の表示 %>
                                <td>
                                    <%= truncate(receipt.memo, length: 50, separator: '...') %>
                                </td>

                                <%# 編集/削除など操作ボタン %>
                                <td class="text-end">
                                    <%= link_to '詳細', receipt_path(receipt), class: "btn btn-sm btn-outline-primary" %>
                                </td>
                            </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
        <% end %>
    </div>
</div>



